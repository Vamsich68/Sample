@model IEnumerable<Sample.Models.Employee>

@{
    ViewData["Title"] = "List";
}

<h1>List</h1>
@*<div>
    <label  class="col-sm-2">EmployeeId</label><br>
    <input  class="text-box" asp-validation-for="EmployeeId"></input>
    <a asp-action="Details" asp-route-id="EmployeeId">search</a>
</div>
<input type="text" asp-route-id="EmployeeId" name="Seach ID" id="EmployeeId"></input>
<a asp-action="Details" asp-route-id="EmployeeId" >search</a>
*@
<form  asp-action="List" >
    <div class="input-group">
        
            <input type="text" name="child" placeholder="Search" aria-label="Search" class="form-control" />
            <input type="submit" value=" search" id="basic-text1"/>
            
    </div>
         
 
</form>
@section scripts{
    <script>
    $(function (){
        $("basic-text1").click(function(){
            $("Form").submit();
        })
    });
    </script>
}

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeAge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeDesignation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeDepartment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeAge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeDesignation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeDepartment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
